# elm-state-management
Simple state management using Elm (purely functional language)

# to run api server:
- npm install -g json-server
- json-server --watch db.json -p 3001

# to run elm app:
- elm install
- elm reactor -> go to src directory
